spring:
  application:
    name: Restro Application

  datasource:
    url: jdbc:postgresql://localhost:5433/resto
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update